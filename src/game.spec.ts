import {GameEvent} from './events';

describe('game', () => {
  it('should', () => {
    const events: GameEvent[] = [{
      type:'game-setup',
      players: [{
        id: 'a',
        species: 'eridani-empire',
      }, {
        id: 'b',
        species: 'eridani-empire',
      }]
    }]
  })
})
